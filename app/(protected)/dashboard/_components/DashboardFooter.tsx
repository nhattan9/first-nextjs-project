export default function DashboardFooter() {
    return (
        <footer className="mt-10 border-t border-ink/10 pt-4 text-xs text-ink/40">
            Khu vực quản trị — chỉ hiển thị trong route group (protected).
        </footer>
    );
}